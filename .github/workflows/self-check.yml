name: KeyWatch self-check

on: [push, pull_request]

jobs:
  keywatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0      # full history so the history scan works
      - uses: ./
